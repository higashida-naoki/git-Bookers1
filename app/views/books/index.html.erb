<html>
  <head></head>
  <body>
    <p id="strong"></p>
    <h1>Bookers</h1>
    <%= form_with model: @book, url: update_path(@book.id), method: :patch do |f| %>
    <table>
      <thead>
        <tr>
          <th>Title</th>
          <th>Body</th>
        </tr>
      </thead>

      <div>
        <nb>Rails</nb>
        <mb>入門編</mb>
        <bb>
          <a class="show" href="<%= show_path(@book.id) %>">Show</a>
        </bb>
        <bb>
          <a class="edit" href="<%= edit_path(@book.id) %>">Edit</a>
        </bb>
        <bb>
          <%= link_to "Destroy", book_path(@book.id), method: :delete, "data-confirm" => "Are you sure？" %>
        </bb>
      </div>

      <div>
        <nb>fdafdafd</nb>
        <mb>123</mb>
        <bb>
          <a class="show" href="<%= show_path(@book.id) %>">Show</a>
        </bb>
      <bb>
          <a class="edit" href="<%= edit_path(@book.id) %>">Edit</a>
      </bb>
      <bb>
         <%= link_to "Destroy", book_path(@book.id), method: :delete, "data-confirm" => "Are you sure？" %>
      </bb>
    </div>

    <div>
      <nb>123</nb>
      <mb>123</mb>
      <bb>
        <a class="show" href="<%= show_path(@book.id) %>">Show</a>
      </bb>
      <bb>
        <a class="edit" href="<%= edit_path(@book.id) %>">Edit</a>
      </bb>
      <bb>
        <%= link_to "Destroy", destroy_path(@book.id), method: :delete, "data-confirm" => "Are you sure？" %>
      </bb>
    </div>

    <div>
    <nb>book</nb>
    <mb>a</mb>
    <bb>
      <a class="show" href="<%= show_path(@book.id) %>">Show</a>
    </bb>
    <bb>
      <a class="edit" href="<%= edit_path(@book.id) %>">Edit</a>
    </bb>
    <bb>
      <%= link_to "Destroy", destroy_path(@book.id), method: :delete, "data-confirm" => "Are you sure？" %>
    </bb>
  </div>

   <div>
      <nb>リーダブルコード</nb>
      <mb>より良いコードを書くためのシンプルで実践的なテクニック</mb>
      <bb>
        <a class="show" href="<%= show_path(@book.id) %>">Show</a>
      </bb>
      <bb>
        <a class="edit" href="<%= edit_path(@book.id) %>">Edit</a>
      </bb>
      <bb>
        <%= link_to "Destroy", destroy_path(@book.id), method: :delete, "data-confirm" => "Are you sure？" %>
      </bb>
    </div>
    
    <div>
    <nb>トラブル知らずのシステム設計</nb>
    <mb>システム設計の要点を図解で説明してくれます</mb>
    <bb>
      <a class="show" href="<%= show_path(@book.id) %>">Show</a>
    </bb>
    <bb>
      <a class="edit" href="<%= edit_path(@book.id) %>">Edit</a>
    </bb>
    <bb>
      <%= link_to "Destroy", destroy_path(@book.id), method: :delete, "data-confirm" => "Are you sure？" %>
    </bb>
  </div>

      <div>
        <nb>たのしいruby</nb>
        <mb>Rubyの入門におすすsめです</mb>
        <bb>
          <a class="show" href="<%= show_path(@book.id) %>">Show</a>
        </bb>
        <bb>
          <a class="edit" href="<%= edit_path(@book.id) %>">Edit</a>
        </bb>
        <bb>
          <%= link_to "Destroy", book_path(@book.id), method: :delete, "data-confirm" => "Are you sure？" %>
        </bb>
      </div>

      <div>
        <nb>入門Git</nb>
        <mb>gitについての基本方法から内部構造まで解説してくれます</mb>
        <bb>
          <a class="show" href="<%= show_path(@book.id) %>">Show</a>
        </bb>
        <bb>
          <a class="edit" href="<%= edit_path(@book.id) %>">Edit</a>
        </bb>
        <bb>
          <%= link_to "Destroy", book_path(@book.id), method: :delete, "data-confirm" => "Are you sure？" %>
        </bb>
      </div>

      <div>
        <nb>アルゴリズム図鑑</nb>
        <mb>アルゴリズムについてカラーイラストでしっかり解説してくれます</mb>
        <bb>
          <a class="show" href="<%= show_path(@book.id) %>">Show</a>
        </bb>
        <bb>
          <a class="edit" href="<%= edit_path(@book.id) %>">Edit</a>
        </bb>
        <bb>
          <%= link_to "Destroy", book_path(@book.id), method: :delete, "data-confirm" => "Are you sure？" %>
        </bb>
      </div>

      <div>
      <nb>達人プログラマー</nb>
      <mb>プログラマ入門は必読！</mb>
      <bb>
        <a class="show" href="<%= show_path(@book.id) %>">Show</a>
      </bb>
      <bb>
        <a class="edit" href="<%= edit_path(@book.id) %>">Edit</a>
      </bb>
      <bb>
        <%= link_to "Destroy", destroy_path(@book.id), method: :delete, "data-confirm" => "Are you sure？" %>
      </bb>
    </div>

    <div>
      <nb>データベース実践入門</nb>
      <mb>効率的なSQL分を教えてくれます</mb>
      <bb>
        <a class="show" href="<%= show_path(@book.id) %>">Show</a>
      </bb>
      <bb>
        <a class="edit" href="<%= edit_path(@book.id) %>">Edit</a>
      </bb>
      <bb>
        <%= link_to "Destroy", destroy_path(@book.id), method: :delete, "data-confirm" => "Are you sure？" %>
      </bb>
    </div>



  </table>

    <h2>New book</h2>
    
      <h4>Title</h4>
        <%= f.text_field :Title %>

      <h4>Body</h4>
        <%= f.text_area :Body %>

      <h4></h4>
        <%= f.submit 'Create Book' %>
    <% end %>
  </body> 
</html>
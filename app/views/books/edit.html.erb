<html>
  <body>
    <h1>Editing Book</h1>
   <%= form_with model: @book, url: update_path(@book.id), method: :patch do |f| %>
          <%= f.text_field :Title %>
	
        <h4>Body</h4>
          <%= f.text_area :Body %>
	
        <h4></h4>
          <%= f.submit 'Update Book' %>

        </p>
          <a class="show" href="<%= show_path(@book) %>">Show</a>
          " | "
          <a class="back" href="/books">Back</a>
      <% end %>
  </body>
</html>